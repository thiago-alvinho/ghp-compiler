def main {
    var matriz[2][2] = {"luiz,", "trouxe", "o", "cafe?"};
    var i;
    var j;

    for(i = 0; i < 2; i++) {
        for(j = 0; j < 2; j++) {
            output(matriz[i][j], " ");
        }
    }
}